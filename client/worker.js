this.addEventListener('push', function(e) {
    const data = e.data.json();
    console.log("recived....");
    this.registration.showNotification(
 //data.title,
        //{
        //    body: data.body,
       // }
      title,
      Option
    );
})